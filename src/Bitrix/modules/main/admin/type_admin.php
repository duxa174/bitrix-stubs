<?php

/**
 * 	@global $find
 * 	@global $find_type
 * 	@global $find_type_id
 * 	@global $find_tmpl_id
 * 	@global $find_event_type
 */
$arFilterFields = array("find", "find_type", "find_type_id", "find_tmpl_id", "find_event_type");